org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# The bundled Android Studio JBR is Java 25, which the current Kotlin
# Gradle plugin's internal JavaVersion parser doesn't understand yet
# (IllegalArgumentException: 25.0.2). Use JDK 21 for Gradle itself instead.
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.12.101-hotspot
# Kotlin's incremental-compilation cache computes relative paths between the
# project (D:) and the Flutter pub cache (C:) — File.relativeTo() throws
# "this and base files have different roots" across drive letters on
# Windows, crashing the image_picker_android compile step. Disabling
# incremental compilation avoids that code path entirely.
kotlin.incremental=false
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
